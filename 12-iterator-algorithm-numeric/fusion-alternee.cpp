// WIP

#include <iostream>
#include <vector>
using namespace std;

template<typename T, typename end1, typename begin2, typename end2>
vector<int> fusion_alternee(vector<int> v1, vector<int> v2) {
   vector<int> result;
   for(int i = 0; i < (v1.size() + v2.size()); ++i) {
      if(v1.at(i / 2 < v1.size())) result.push_back()
   }
}


int main() {

return EXIT_SUCCESS;
}